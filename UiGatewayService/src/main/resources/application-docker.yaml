spring:
  application:
    name: UiGatewayService
  data:
    mongodb:
      uri: mongodb://mongo:27017/ui-gateway
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
server:
  port: 8080
